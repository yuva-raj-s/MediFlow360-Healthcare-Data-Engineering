# Naming Conventions
- Bronze files: `<entity>_YYYYMMDD_HHMMSS.parquet`
- Notebooks: `<Sequence>_<Layer>_<Function>_NB.py` (e.g., `02b_Silver_SCD2_NB.py`)
- Tables: `dim_` for dimensions, `fact_` for facts.